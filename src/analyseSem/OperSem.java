package analyseSem;

import org.antlr.runtime.tree.Tree;

public class OperSem {
	
	//On suppose que le node entré est un opérateur
	public OperSem(Tree node)
	{
		String type = node.getText();
		
		
	}
}
